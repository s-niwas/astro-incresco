---
import type { postHeading } from "~/types";

export interface PostHeading {
  content?: any[];
  heading: postHeading;
}

const { textContent, ...item } = Astro.props;
---

<section class="my-2" set:html={textContent} />
